apiVersion: v1
kind: Pod
metadata:
  name: Secret-pod
spec:
  containers:
  - name: Secret-pod
    image: nginx
    ports:
    - containerPort: 80
    envFrom:
      - secretRef:
          name: devops-secrets